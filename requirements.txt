python-telegram-bot>=20.0
python-dotenv>=0.19.0
requests>=2.28.0
aiogram==3.3.0
fastapi==0.110.0
geopy==2.4.0
sqlite3  # Built-in, no need to specify a version
uvicorn==0.29.0  # Needed to run FastAPI
asyncio  # Built-in, no need to specify a version
logging  # Built-in, no need to specify a version
